import React from "react";
import { herodesktop, heromobile } from "../images";

const Hero = () => {
  return (
    <div>
      <img src={herodesktop} alt="desktop" className="" />
    </div>
  );
};

export default Hero;
